import 'package:fitness/screens/program/program_page.dart';

var appPages = [
  const ProgramPage(),
];

var appRoutes = {
  ProgramPage.id: (context) => const ProgramPage(),
  /*ProgramPage.id: (context) => const ProgramPage(),*/
};
